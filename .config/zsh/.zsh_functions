# -------------------------------
# Matt's Functions
# -------------------------------

# Make a directory and jump into it
mkcd () { mkdir -p "$1" && cd "$1"; }

# Extract different types of archives
extract () {
  if [ -f "$1" ]; then
    case "$1" in
      *.tar.bz2)   tar xjf "$1"   ;;
      *.tar.gz)    tar xzf "$1"   ;;
      *.bz2)       bunzip2 "$1"   ;;
      *.rar)       unrar x "$1"   ;;
      *.gz)        gunzip "$1"    ;;
      *.tar)       tar xf "$1"    ;;
      *.tbz2)      tar xjf "$1"   ;;
      *.tgz)       tar xzf "$1"   ;;
      *.zip)       unzip "$1"     ;;
      *.Z)         uncompress "$1";;
      *.7z)        7z x "$1"      ;;
      *)           echo "'$1' cannot be extracted via extract()" ;;
    esac
  else
    echo "'$1' is not a valid file"
  fi
}

help() {
  figlet -f small "Matt's CLI" | lolcat

  CYAN='\033[1;36m'
  YELLOW='\033[1;33m'
  RESET='\033[0m'

  echo ""
  printf "${YELLOW}  Projects${RESET}\n"
  printf "  ${CYAN}ecv${RESET}           →  ~/dev/ecv\n"
  printf "  ${CYAN}lakbaygo${RESET}      →  ~/dev/lakbaygo\n"
  printf "  ${CYAN}ark${RESET}           →  ~/dev/ark\n"
  printf "  ${CYAN}betterbacolod${RESET} →  ~/dev/betterbacolod\n"

  echo ""
  printf "${YELLOW}  Git${RESET}\n"
  printf "  ${CYAN}gs${RESET}   →  git status\n"
  printf "  ${CYAN}ga${RESET}   →  git add .\n"
  printf "  ${CYAN}gc${RESET}   →  git commit -m\n"
  printf "  ${CYAN}gp${RESET}   →  git push\n"
  printf "  ${CYAN}gl${RESET}   →  git log graph\n"
  printf "  ${CYAN}gco${RESET}  →  git checkout\n"
  printf "  ${CYAN}gcb${RESET}  →  git checkout -b\n"

  echo ""
  printf "${YELLOW}  Files & Search${RESET}\n"
  printf "  ${CYAN}ll${RESET}     →  detailed list (eza + git status)\n"
  printf "  ${CYAN}la${RESET}     →  list all including hidden\n"
  printf "  ${CYAN}lt${RESET}     →  tree view 2 levels\n"
  printf "  ${CYAN}cat${RESET}    →  syntax highlighted (bat)\n"
  printf "  ${CYAN}ff${RESET}     →  fuzzy file finder with preview\n"
  printf "  ${CYAN}fh${RESET}     →  fuzzy history search\n"
  printf "  ${CYAN}ctrl+r${RESET} →  fuzzy history in prompt\n"
  printf "  ${CYAN}ctrl+t${RESET} →  fuzzy file paste\n"

  echo ""
  printf "${YELLOW}  Config${RESET}\n"
  printf "  ${CYAN}conf${RESET}    →  edit zshrc\n"
  printf "  ${CYAN}aconf${RESET}   →  edit aliases\n"
  printf "  ${CYAN}gconf${RESET}   →  edit greeting\n"
  printf "  ${CYAN}sshconf${RESET} →  edit SSH config\n"
  printf "  ${CYAN}rld${RESET}     →  reload zsh\n"
  printf "  ${CYAN}mhelp${RESET}   →  show this again\n"

  echo ""
  printf "${YELLOW}  Tools${RESET}\n"
  printf "  ${CYAN}rtk gain${RESET}     →  token savings stats\n"
  printf "  ${CYAN}rtk discover${RESET} →  missed savings opportunities\n"
  echo ""
}

